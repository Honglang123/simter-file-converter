simter-file-converter:
  version: @project.version@
  rest-context-path: /file-converter
spring.main.banner-mode: 'off'
logging:
  file.name: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: ERROR
    tech.simter.file: ERROR